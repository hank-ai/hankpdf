"""Verifier package (Phase 0 scaffold).

Phase 2 populates:

- ``ocr_diff.py`` — Levenshtein + reading-order-insensitive comparator
- ``digit_match.py`` — digit-multiset exact-match gate
- ``ssim.py`` — global + tile-level SSIM
- ``structural.py`` — page count / annot / form / signature audit
"""
